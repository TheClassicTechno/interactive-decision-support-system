{
  "version": 2,
  "buildCommand": "bash copy-deps.sh && if [ -f pyproject.toml ]; then echo 'Found pyproject.toml, Vercel should install dependencies automatically'; fi && npm install && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/backend-api/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "functions": {
    "api/**/*.py": {
      "runtime": "@vercel/python"
    },
    "api/index.py": {
      "maxDuration": 300,
      "memory": 3008
    }
  },
  "env": {
    "PYTHONPATH": "/var/task"
  }
}
