# LangGraph and LangChain dependencies
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0  # For OpenAI models
langchain-anthropic>=0.2.0  # For Anthropic models (optional)
langchain-community>=0.3.0  # For SQL database tools

# Database
sqlalchemy>=2.0.0  # For SQL database operations
neo4j>=5.20.0  # For Neo4j knowledge graphs

# API and HTTP
requests>=2.31.0
beautifulsoup4>=4.12.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
sse-starlette>=1.6.0  # For Server-Sent Events (streaming progress updates)
mangum>=0.17.0  # For Vercel serverless function compatibility

# Python typing extensions
typing-extensions>=4.8.0

# Environment variables
python-dotenv>=1.0.0

# Configuration and templating
PyYAML>=6.0.0  # For YAML configuration files
Jinja2>=3.1.0  # For prompt templating

# PDF parsing (for scraping manufacturer documentation)
pdfplumber>=0.10.0  # Preferred PDF parser (better text extraction)
PyPDF2>=3.0.0  # Fallback PDF parser

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0
pytest-timeout>=2.2.0
httpx>=0.26.0  # For FastAPI test client
